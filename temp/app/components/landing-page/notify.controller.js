(function() {
    angular.module('fittanyUiApp')
        .controller('NotifyController',['$scope','$http','$state',

            function($scope,$http,$state) {
               

            }

        ]);
})();
